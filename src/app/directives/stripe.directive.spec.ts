import { StripeDirective } from './stripe.directive';

describe('StripeDirective', () => {
  it('should create an instance', () => {
    const directive = new StripeDirective();
    expect(directive).toBeTruthy();
  });
});
